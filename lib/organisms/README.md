# organisms/

Coherent UI sections — headers, navigation rails, a work-item card with
actions, a filterable list. Organisms compose molecules and receive context-
style dependencies (controllers, repositories) from templates.

Per the initial agreement: **templates bind UI models to organisms.** An
organism declares the UI model shape it needs; the template adapts the page
state to that shape.
